var values = [1, 2, 3, 4, 5, 6];

for(var i = 0; i < values.length; i++) {
  console.log(values[i]);
}

values.forEach(function(e) { console.log(e); });

//Michael Feather

//In OO we encapsulate the moving parts
//in FP we eliminate the moving parts