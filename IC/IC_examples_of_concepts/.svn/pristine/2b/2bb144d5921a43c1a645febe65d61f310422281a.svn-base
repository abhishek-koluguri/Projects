class Car
  constructor: (year) ->
    @year = year
    @miles = 0
  
  drive: (dist) ->
    @miles += dist

Car::turn = () -> console.log 'turning...'
#:: means the function is on protytype than on the constructor itself
car = new Car(2015)
car.turn()