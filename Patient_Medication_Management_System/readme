Steps to start the app and use it:

In cmd, navigate the pmms project and run the following commands: npm install, npm install express
Download mongodb and have it running.
Now run the following command to start the nodeJS server: node ./bin/www
Once the app server is started, open up the browser and browse localhost:3000 and you should be the SPA.

Things to do:

Front-End:

Back-End:

Exception handling that can make the app server crash.
TenantID
Tests for all api calls
see if toArray is needed
Authentication mechanism overview
